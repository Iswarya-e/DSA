Public:
    ListNode* reverseList(ListNode* head) {
        ListNode* temp=head;
        ListNode* newList=NULL;
        while(temp!=NULL)
        {
            ListNode* newHead=temp->next;
            temp->next=newList;
            newList=temp;
            temp=newHead;
        }
        return newList;
    }
};
