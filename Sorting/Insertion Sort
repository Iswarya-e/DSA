int j;
        for(int i=0;i<n;i++)
        {
            j=i;
            while(j>0 and nums[j-1]>nums[j])
            {
                swap(nums[j-1],nums[j]);
                j--;
            }
        }
