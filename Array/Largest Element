 int largest(vector<int> &arr, int n)
    {
        int max=INT_MIN;
        //O(N)
        for(int i=0;i<n;i++)
        {
            if(max<arr[i]) max=arr[i];
        }
        return max;
        
    }
