vector<int> superiorElements(vector<int>&a) {
    vector<int> ans;
    // Write your code here.
    int  maxele=INT_MIN;
    for(int i=a.size()-1;i>=0;i--)
    {
        if(a[i]>maxele)
        {
            ans.push_back(a[i]);
            maxele=a[i];
        }
    }
    return ans;
}
